# dsh-evolution 第十轮评审（续）：`011-maintenance-subagent-v2.md` 评估与检验报告

| 项 | 内容 |
|---|---|
| 评审对象 | `docs/design-review/011-maintenance-subagent-v2.md`（v9 评审修订版；取代 010；**未入库**） |
| 核对基准 | 插件 HEAD `a5bb19b`（v7 全部修复已落地）+ 上游 0.1.1-rc.2 平台事实 + 本机真实技能库复测 |
| 评审方法 | ① v9 全部 12 项意见的**裁决质量逐条核验**（采纳是否落到位、裁决理由是否成立）；② 修订后全文的**新一致性与新缺陷**排查；③ §1 测量契约的**实机复测**（含 011 对旧数字出处的假说验证）；④ 与 007-009 惯例及 v8 缓存纪律的终检 |
| 约束 | 只读评审，未修改任何文件 |

**评审结论：批准（approve）——011 达到可启动 Phase 1 的成熟度。** v9 的 12 项意见**全部获得实质回应**，且多数裁决质量高于"照单全收"：F-1 用"新增 restructure 用户命令 + B2-B5 走 patch 指引 + 不为 B3 松绑 ambiguous 保护"的三分处理，A4 用"事件载荷 + runId 回引、不建 state 表"的成本裁定，F-4 用"镜像实现语义、不引入参数化分叉"，A8 用"事实面/语义面二层各取所需"——每处都有可检验的理由。**未发现新的 P1/P2**；遗留问题集中在测量契约的口径混标与若干 P3 级不一致（§3），不阻塞实施，建议随手批。

---

## 1. v9 意见回应核验（12/12 有裁决，11 项落到位）

| v9 项 | 裁决 | 核验结果 |
|---|---|---|
| F-1 执行映射缺口 | 采纳，分层处理 | ✅ §9 映射表：新增 `/evolution restructure`（桥接既有内核，两阶段/origin gate 不动）；B2-B5 走 patch 指引；A2 改名降级为"仅报告 + needs_human"。**关键裁决正确**：不为 B3 松绑 restructure 的 ambiguous-anchor 保护（那是语义保护的既有行为），patch 不做机械生成（fuzzy 补丁链前科引证准确） |
| A1 signals 归属 | 采纳 | ✅ `evolution-core/src/drift-signals.ts` 与既有 `signals.ts`（review 门）区名分立；core=纯函数层，maintenance 只编排；D1 依赖单向与 D4 共享同时成立（§4/§12-D1） |
| F-2 工具面 | 采纳 | ✅ `['skill','maintenance_probe']`（§3/§6/§7/§13 四处一致） |
| F-4 prefix_cluster 阈值 | 裁决 ≥2 镜像实现 | ✅ 合理——参数化反而制造"文档阈值 vs 实现阈值"第二分叉；§4 表就地钉死 |
| A5 信任边界 | 采纳 | ✅ §8 威胁模型小节成立：外送面、注入现实（技能正文=模型自写）、四道防护链（redact 下沉 core 供事实块/probe 共用、plan 无执行权、用户挑选、confidence≤0.4 可见化）+ 测试矩阵 |
| A4 plan 短暂性 | 裁决：事件载荷摘要 + runId，不建 state 表 | ✅ 理由成立（瞬态审查产物不值得新持久化面）；两处措辞过强见 §3-(3) |
| A6 并发/超时 | 采纳 | ✅ `maintainRunning` 门（照抄 curator `:402`）+ `maintainTimeoutMs` 120s（照抄 review `:52/:227`），含 already-running 语义 |
| A7 root/quality_low | 采纳 | ✅ root 入三处对齐故事；quality_low 窗口未开=unknown；模板加"该技能所有结构建议默认 needs_human"——**门与校验器的衔接有一处歧义**，见 §3-(4) |
| A2 评估面总表 | 采纳 | ✅ §5 四面总表 + "新增评估面先登记"契约；表内一处笔误见 §3-(6) |
| A3 bundle 演进 | 裁决 KEEP + 记录 | ✅ 两个备选的反驳成立（verifyPromptBundle 单一契约改分段触面=全部消费方；自持小 bundle 拆散联合签名）；同版发布纪律如实 |
| A8 备选论证 | 采纳 | ✅ §12 三备选逐一裁决，"0 token 只能得到事实，得不到裁决"的答辩成立；`--facts` 零 token 事实面是加分设计 |
| F-3/F-5/F-6 | 采纳 | ✅ "4 个可验收增量"措辞统一；confidence 两区间 + needs_human 机械判据（消隔离带）；测量契约建立——**但契约本身仍有口径混标**，见 §2 |

---

## 2. §1 测量契约实机复测（本节全部数字为本次实跑）

| 011 声称 | 实测 | 判定 |
|---|---|---|
| 单技能 `dsh-evolution-maintenance/SKILL.md` = 105,367B | 105,367B | ✅ 精确 |
| 库级 6 个 SKILL.md 合计 131.6KB | 131,578B | ✅ 精确 |
| **"rc 引用：`rc\.\d+` 全量口径 168"** | **168 = 单技能值**；库级 SKILL.md 合计 = **174**；全 .md = 491 | ❌ **口径混标**：168 被标为"全量"，实为单技能 |
| 日期 40 | 单技能 40 ✅；库级更多 | ⚠️ 同上（未标口径） |
| "全部文件 41 个" | 排除 .archive/.backups 后 **28 个** | ❌ 数字对不上（疑混入归档计数，需标口径） |
| 旧数字"153"出处 = `rc\.\d{2,}` 漏一位数 | 该技能上 `rc\.[0-9]{2,}` **恰好 = 153** | ✅ **假说精确验证**——011 对 v9 F-5 的自校完全成立 |
| 双标题（29-30 行）/ 3 长行（6889/2131/1672） | 精确 | ✅ |

**判定**：单技能口径的四个数字全部精确可复算（F-5 的核心诉求达成）；但"全量口径 168"与"41 个文件"两处**再次混标**——同一类缺陷（口径/范围标注）在修正稿中残存。修正成本一行：把 §1 改为"单技能口径（契约锚）：105,367B/168/40/…；库级参考：131.6KB/174/…"。另外 **Phase 1 验收仍钉死活数字**（"168 rc/40 日期…全中"）——010→011 之间该数字已从 153 漂到 168，证明它必然继续漂；验收应改为"fixture 精确 + 真实库触发"，否则验收随库维护被动失效。

---

## 3. 新发现问题（全部 P3）

1. **quality_low 门与校验器判据的衔接歧义**：模板 §3 规定"quality_low=unknown 时该技能所有结构建议默认 needs_human:true"，但 §4 的机械判据 `needs_human = confidence<0.6 OR 不可逆 OR is_override OR 引用 unknown 信号` **不包含 quality_low**——结构建议的证据通常不引用环境信号，校验器无法从 plan 本身机械判定该默认是否被遵守。"默认"可被什么覆盖（is_override？）也未说。需一句话落进 validate-plan（校验器读事实块的 quality_low 状态并全局施加）。
2. **`reversibility` 枚举与 patch 指引错位**：模板枚举 `archive|restructure|rename|none`，而 §9 把 B2-B5 定为"patch 可逆（有备份）"——patch 建议在枚举里只能填 `none`，与"可逆"矛盾。枚举加 `patch` 值即可。
3. **§10 "plan 全文可通过 --plan 回查"措辞过强**：事件载荷存的是**摘要**，回查得到摘要链而非全文；"全文重跑"才是全文。改为"摘要链 + 重跑再生"即可。
4. **`--plan` 审计链受事件保留窗约束**：events.json 轮转保留 10 归档 + 4000 active（007）——低频命令下实际无虞，但"审计断链闭合"的承诺应标注保留窗边界。
5. **§9 A2 归档执行通道写"curator run"**：curator run 是阈值驱动的全库生命周期 pass，不能定向归档某个技能；定向归档的现实路径是会话 agent 调 `skill_manage delete`（文档已列）——"curator run"应从该行删去或改注"仅间接（阈值命中时）"。
6. **§5 表笔误**：skill-health 行阈值列 "softBodyChars/stampDensity/**spawn** 等"——`spawn` 不是 SkillHealthThresholds 字段（应为 `churnMinPatches`）。
7. **命令签名只支持单 move**：`/evolution restructure <name> <heading> <to_file>` 一次一条；MAX_RESTRUCTURE_MOVES=5 的多 move 场景需多次调用（内核 append 语义可兼容，但文档宜写明"多次调用叠加"或允许多对参数）。

---

## 4. 架构一致性终检（修订后复检）

| 轴 | 判定 |
|---|---|
| 分层/依赖方向 | ✅ D1 修订后自洽：drift-signals 落 core（纯函数层，与 quality/skill-health 同层）、maintenance 只编排、commands→maintenance→core 单向；D4 共享经 core 自然成立 |
| 评估面格局 | ✅ §5 总表落地，四面的触发/信号源/输出去向/权限/时间线一次说清；"新增评估面先登记"是防分叉的正确机制 |
| 信任边界 | ✅ §8 与 review 姿态对齐（redact 同构、测试矩阵复用模式）；redact 下沉为纯函数迁移、review 改 import、无新依赖边 |
| 数据流 | ✅ A4 裁决成本合理；事件载荷 + runId + `--plan` 回引构成最小审计闭环（保留窗边界见 §3-4） |
| 执行面 | ✅ 唯一新增面 = restructure 用户命令（复用内核）；ambiguous-anchor 保护的"不松绑"裁决正确——拒绝重复标题是防错机制不是缺陷 |
| bundle 演进 | ✅ §11 的 KEEP 裁定理由充分（单一校验契约 + 同版发布惯例），风险表更正了 v9 指出的"旧 worker"措辞 |
| 发布/流程 | ⚠️ 010/011/v9 报告三者仍**均未入库**（git 未跟踪）——011 自称"取代 010"，两份设计稿应一起提交并按惯例在 REVIEWS.md 记录评审结论 |

---

## 5. 结论与统计

**判定：批准（approve），Phase 1 可启动。** 011 是一次高质量修订：12 项意见全部有裁决、11 项落到位、1 项（A4）的"不建表"裁决有明确成本理由；对 v9 F-5 的自校（153 = `rc\.\d{2,}` 假说）经实机验证**精确成立**，这种"先解释旧数字再定义新契约"的做法正是 §2-P4 要求的溯源纪律。剩余 7 项 P3（口径混标 2 处、枚举/判据衔接 2 处、措辞/笔误 3 处）均不阻塞，建议随 Phase 1 的首个 PR 顺手清理；文档三者（010/011/评审报告）应先入库再开工。

**统计**：v9 意见回应核验 12 项（11 ✅ / 1 ⚠️ 落点有歧义）；§1 契约数字复测 7 项（4 ✅ 精确 / 1 假说验证成立 / 2 ❌ 口径混标）；新发现 7 项 P3 + 1 项流程提醒。十轮评审累计意见 93 项。
